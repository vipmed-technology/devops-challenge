apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: api-gateway
          image: devops-challenge/api-gateway:local
          imagePullPolicy: IfNotPresent
