# Override ExternalSecret to point to the prod secret path in AWS Secrets Manager
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: redis-credentials
  namespace: devops-challenge
spec:
  data:
    - secretKey: password
      remoteRef:
        key: devops-challenge/prod/redis-password
        property: password
